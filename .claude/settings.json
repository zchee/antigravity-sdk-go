{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; case \"$f\" in *.go) gofumpt -w -extra \"$f\" && goimports -w \"$f\";; esac; } 2>/dev/null || true",
            "statusMessage": "Formatting Go (gofumpt + goimports)..."
          }
        ]
      }
    ]
  }
}
